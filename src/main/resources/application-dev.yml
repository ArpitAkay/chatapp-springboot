spring:
  devtools:
    add-properties: true
  application:
    name: chatapp-springboot
  datasource:
    url: jdbc:mysql://localhost:3306/chatapp
    username: root
    password: root@123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect